[package]
name = "lazy_engine_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "lazy_engine_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
arrow = { version = "53", features = ["pyarrow"] }
rayon = "1"
hashbrown = "0.14"
